let links = document.querySelectorAll('[data-app-link]');
links.forEach((link) => {
  link.addEventListener('click', (e) => {
    e.preventDefault();
    e.target.nextElementSibling.classList.toggle('accordion--active');
  });
});
